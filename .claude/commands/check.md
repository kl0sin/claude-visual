Run full project checks: TypeScript type checking and all tests.

1. Run `bunx tsc --noEmit` to verify there are no type errors.
2. Run `bun test` to execute all tests.
3. Report results — list any errors clearly with file paths and line numbers.
